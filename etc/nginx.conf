server {
     
    listen 80;
    server_name localhost;

    location /uploads/ {
    alias /home/box/web/uploads;
    }
    
    location ~* \.\w+$ {
    root /home/box/web/public;
    }
    
    location ~* \w+$ {
    return 404;
    }
}
